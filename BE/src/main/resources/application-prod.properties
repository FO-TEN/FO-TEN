# 프로덕션용. 기본값을 두지 않는다 — 값이 없으면 기동 시점에 바로 실패해야
# 잘못된 기본값으로 조용히 뜨는 사고를 막을 수 있다. 환경변수로만 채운다.
db.url=${DB_URL}
db.username=${DB_USERNAME}
db.password=${DB_PASSWORD}

exchange.rate.cron=${EXCHANGE_RATE_CRON}
exchange.rate.base-url=${EXCHANGE_RATE_BASE_URL}

llm.api-key=${LLM_API_KEY}
llm.model=${LLM_MODEL}
llm.base-url=${LLM_BASE_URL}
