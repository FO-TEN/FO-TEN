<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foten.goal.mapper.TransactionSummaryMapper">

    <select id="findCumulativeSavingsPayment" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(amount), 0)
        FROM transaction_history
        WHERE member_id = #{memberId}
          AND transaction_type = 'SAVINGS_PAYMENT'
          AND transaction_at &gt;= #{since}
    </select>

    <select id="findBalanceAsOf" resultType="java.math.BigDecimal">
        SELECT balance_after
        FROM transaction_history
        WHERE member_id = #{memberId}
          AND transaction_at &lt;= #{asOf}
        ORDER BY transaction_at DESC
        LIMIT 1
    </select>

    <select id="findAverageMonthlyExpense" resultType="java.math.BigDecimal">
        SELECT COALESCE(SUM(amount), 0) / #{months}
        FROM transaction_history
        WHERE member_id = #{memberId}
          AND transaction_type = 'EXPENSE'
          AND transaction_at &gt;= #{windowStart}
          AND transaction_at &lt; #{windowEnd}
    </select>

</mapper>
