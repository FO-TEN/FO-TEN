<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foten.product.mapper.AssetSnapshotMapper">

    <select id="selectLatest" resultType="com.foten.product.domain.AssetSnapshotVO">
        SELECT asset_snapshot_id,
               savings_roadmap_id,
               segment_id,
               snapshot_month,
               monthly_payment,
               cash_saving_balance,
               created_at
        FROM asset_snapshot
        WHERE savings_roadmap_id = #{savingsRoadmapId}
        ORDER BY snapshot_month DESC
        LIMIT 1
    </select>

</mapper>
