<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foten.product.mapper.ProductMapper">

    <select id="selectSavingsCandidates" resultType="com.foten.product.domain.ProductRateCandidate">
        SELECT p.product_id,
               p.product_name,
               p.max_rate,
               p.monthly_payment_limit,
               pr.base_rate
        FROM product p
        JOIN product_rate pr ON pr.product_id = p.product_id
        WHERE p.product_type = 'SAVINGS'
          AND p.installment_type = 'FREE'
          AND pr.min_term &lt;= #{termMonths}
          AND (pr.max_term IS NULL OR pr.max_term &gt;= #{termMonths})
    </select>

    <select id="selectDepositCandidates" resultType="com.foten.product.domain.DepositRateCandidate">
        SELECT p.product_id,
               p.product_name,
               p.max_rate,
               pr.base_rate,
               p.min_subscription_amount
        FROM product p
        JOIN product_rate pr ON pr.product_id = p.product_id
        WHERE p.product_type = 'DEPOSIT'
          AND pr.min_term &lt;= #{termMonths}
          AND (pr.max_term IS NULL OR pr.max_term &gt;= #{termMonths})
    </select>

    <select id="selectByIds" resultType="com.foten.product.domain.ProductVO">
        SELECT product_id,
               product_name,
               product_type,
               installment_type,
               monthly_payment_limit,
               max_rate,
               min_subscription_amount,
               description
        FROM product
        WHERE product_id IN
        <foreach collection="productIds" item="productId" open="(" separator="," close=")">
            #{productId}
        </foreach>
    </select>

</mapper>
